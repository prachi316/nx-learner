import { ComponentSize, ComponentColor, ComponentVariant } from '../types';

// Default component configurations
export const DEFAULT_COMPONENT_CONFIG = {
  size: 'md' as ComponentSize,
  variant: 'default' as ComponentVariant,
  color: 'gray' as ComponentColor,
};
